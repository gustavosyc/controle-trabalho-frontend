# Frontend - controle-trabalho

## Instalação local

1. Instale dependências:
```
cd frontend
npm install
```
2. Configure a variável VITE_API_URL no .env (ex: VITE_API_URL=http://localhost:3333)
3. Rode:
```
npm run dev
```
